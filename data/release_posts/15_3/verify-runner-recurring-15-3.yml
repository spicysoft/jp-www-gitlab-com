features:
  secondary:
    - name: "GitLab Runner 15.3"
      available_in: [core, premium, ultimate]
      documentation_link: 'https://docs.gitlab.com/runner'
      reporter: deastman
      stage: verify
      categories:
        - 'GitLab Runner Core'
      description: |
        We’re also releasing GitLab Runner 15.3 today! GitLab Runner is the lightweight, highly-scalable agent that runs your CI/CD jobs and sends the results back to a GitLab instance. GitLab Runner works in conjunction with GitLab CI/CD, the open-source continuous integration service included with GitLab.

        #### What's new:
        
        - [Use Podman for the Docker executor on Linux](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/27119)
        
        #### Bug Fixes:

        - [Artifact downloads fail with certain `CI_JOB_TOKEN` values](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/29193)
        - [GitLab Runner Operator for Kubernetes does not install in offline environments](https://gitlab.com/gitlab-org/gl-openshift/gitlab-runner-operator/-/issues/77)
        
        The list of all changes is in the GitLab Runner [CHANGELOG](https://gitlab.com/gitlab-org/gitlab-runner/blob/15-3-stable/CHANGELOG.md).
